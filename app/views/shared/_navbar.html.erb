<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand" do %>
      <% if Village.setup_complete? && Village.any? %>
        <%= Village.first.name %>
      <% else %>
        Villagers
      <% end %>
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <% if Village.setup_complete? && Village.any? %>
            <% village = Village.first %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="villageDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Village
              </a>
              <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="villageDropdown">
                <li>
                  <%= link_to "View Village", village_path, class: "dropdown-item" %>
                </li>
                <% if current_user.village_admin? %>
                  <li><hr class="dropdown-divider"></li>
                  <li>
                    <%= link_to "Edit Village Settings", edit_village_path, class: "dropdown-item" %>
                  </li>
                <% end %>
              </ul>
            </li>
          <% end %>
          <li class="nav-item">
            <%= link_to "Profile", edit_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link", data: { turbo_method: :delete } %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

